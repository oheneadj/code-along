import React from 'react'

const prop = (props) => {
  return (
    <div>{props.name}</div>
  )
}

export default prop




<prop name="prince"/>

function app(a, b, c) {
    return a + b + c;
}
app(23, 34, 67);